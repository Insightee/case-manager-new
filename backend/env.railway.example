# Railway API service — paste into Project → Service (backend) → Variables
# Project ID: ead85fb6-1826-4eed-bad9-2513e89c4854 (truthful-blessing)
# Do NOT set SMTP on Vercel. Set VITE_API_URL on Vercel only (see frontend/vercel-env.example).

APP_ENV=production
SEED_DEMO_DATA=false

# Reference the Postgres plugin (Railway variable reference syntax):
# DATABASE_URL=${{Postgres.DATABASE_URL}}
# REDIS_URL=${{Redis.REDIS_URL}}

JWT_SECRET_KEY=<long-random-string>
JWT_REFRESH_SECRET_KEY=<different-long-random-string>

# Vercel frontend URL (no trailing slash) — must match CORS entry below
FRONTEND_URL=https://frontend-insightes-projects.vercel.app
CORS_ORIGINS=http://localhost:5173,https://frontend-insightes-projects.vercel.app

# ZeptoMail SMTP (API only)
EMAIL_PROVIDER=zeptomail
SMTP_HOST=smtp.zeptomail.com
SMTP_PORT=587
SMTP_USER=emailapikey
SMTP_PASSWORD=<zeptomail-send-token-secret>
SMTP_TLS=true
SMTP_FROM_EMAIL=noreply@insighte.in
# Leave billing/verification unset unless those addresses are verified in ZeptoMail Mail Agent
# SMTP_FROM_BILLING_EMAIL=billing.noreply@insighte.in
# SMTP_FROM_VERIFICATION_EMAIL=verification.noreply@insighte.in
SMTP_FROM_NAME=Insighte

PASSWORD_RESET_EXPIRE_HOURS=1
PASSWORD_RESET_RATE_LIMIT_PER_HOUR=3

STORAGE_PROVIDER=r2
STORAGE_PREFIX=insightcase
STORAGE_ENVIRONMENT=production
# Cloudflare account ID (Dashboard → right sidebar). Example bucket: insightecase
R2_ACCOUNT_ID=0409ea66ef188ae9783c5e9aa7af9445
R2_ACCESS_KEY_ID=<32-char-from-r2-manage-api-tokens>
R2_SECRET_ACCESS_KEY=<secret-from-r2-manage-api-tokens>
R2_BUCKET_NAME=insightecase
R2_ENDPOINT_URL=https://0409ea66ef188ae9783c5e9aa7af9445.r2.cloudflarestorage.com
# cfat_* Cloudflare account tokens are NOT valid as R2_ACCESS_KEY_ID — see docs/CLOUDFLARE_R2.md
